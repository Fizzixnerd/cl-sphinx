(cl:in-package :ps)

(defclass segmentation (ps-utils:iterator)
  (seg-ptr))

(defmethod next ((segmentation segmentation))
  ;; body
  )

(defmethod value ((segmentation segmentation))
  ;; body
  )
